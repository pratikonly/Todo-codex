## Database (choose one provider)
# Vercel Postgres / Neon / Supabase
POSTGRES_URL="postgres://USER:PASSWORD@HOST:PORT/DATABASE"

## Optional Prisma connection string if you use Prisma
POSTGRES_PRISMA_URL="postgres://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

## Example auth secret if you add NextAuth
NEXTAUTH_SECRET="replace-with-strong-secret"
NEXTAUTH_URL="http://localhost:3000"
